# Environment Variables Example
# Copy to .env for local testing (not needed for Claude Desktop)

# Required: Mistral API key
MISTRAL_API_KEY=your_mistral_api_key_here

# Output directory for markdown files
OCR_OUTPUT_DIR=/Users/hinrayleung/Documents/OCR_Results

# Maximum file size in MB (default: 50)
MAX_FILE_SIZE_MB=50

# Cache settings
OCR_CACHE_ENABLED=true          # Enable/disable result caching (default: true)
OCR_CACHE_TTL_HOURS=168         # Cache TTL in hours, 168 = 7 days (default: 168)
OCR_CACHE_DIR=                 # Optional: override cache directory (default: <OCR_OUTPUT_DIR>/.cache)

# Image extraction settings
OCR_IMAGE_MIN_SIZE=100          # Min dimension to include images (default: 100)

# Batch processing settings
OCR_MAX_CONCURRENT=5            # Max concurrent API requests (default: 5)

# URL fetching settings
OCR_URL_TIMEOUT_SECONDS=30      # HTTP timeout for URL downloads (default: 30)
OCR_URL_MAX_REDIRECTS=3         # Max redirects to follow for URLs (default: 3)
OCR_URL_ALLOW_NONSTANDARD_PORTS=false  # Allow ports other than 80/443 (default: false)

# Advanced settings (usually not needed)
# MISTRAL_API_BASE=https://api.mistral.ai/v1
# MISTRAL_OCR_MODEL=mistral-ocr-latest
# MCP_SERVER_NAME=MistralOCR
# MCP_LOG_LEVEL=INFO
